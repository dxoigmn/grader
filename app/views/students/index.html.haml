.section
  .header
    %h2= 'Students'
  
  .content
    %ul
      - @students.each do |student|
        %li= link_to student.name, student
  
  .footer
    %ul
      %li=link_to 'Add Student', new_student_path
