.section
  .header
    %h2= link_to(@assignment.name, @assignment)
    %h3= link_to('Criteria', assignment_criteria_path(@assignment))
    %h4= 'New Criterion'
  
  .content
    = render('criteria/form', :assignment => @assignment, :criterion => @criterion)
