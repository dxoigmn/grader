%h2= link_to(@student.name, @student)
%h3= link_to(@assignment.name,  [@student, @assignment])
%h4=h @question.title

.help=h @question.help

= render('grades/form', :student => @student, :assignment => @assignment, :question => @question, :grade => @grade)
